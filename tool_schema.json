[
  {
    "description": "Get current weather information for a specified location. Returns temperature (automatically converted to Celsius or Fahrenheit based on the country), WMO weather code, and wind speed in km/h. Supports city names, addresses, or place names worldwide.",
    "inputSchema": {
      "description": "Request for weather information",
      "properties": {
        "location": {
          "description": "Location name (city, address, or place)",
          "type": "string"
        }
      },
      "required": [
        "location"
      ],
      "type": "object"
    },
    "name": "get_weather",
    "outputSchema": {
      "description": "Response containing weather information",
      "properties": {
        "location": {
          "description": "Location name",
          "type": "string"
        },
        "temperature": {
          "description": "Temperature value",
          "type": "number"
        },
        "temperature_unit": {
          "description": "The unit of temperature (Celsius or Fahrenheit)",
          "type": "string"
        },
        "weather_code": {
          "description": "WMO weather code",
          "type": "integer"
        },
        "wind_speed": {
          "description": "Wind speed in km/h",
          "type": "number"
        }
      },
      "required": [
        "location",
        "temperature",
        "temperature_unit",
        "weather_code",
        "wind_speed"
      ],
      "type": "object"
    }
  },
  {
    "description": "Get a personalized greeting for the user. This tool demonstrates how the interceptor can extract user information from JWT tokens and pass it to the main Lambda function.",
    "inputSchema": {
      "description": "Input for the personalized greeting tool.",
      "properties": {
        "authorization_token": {
          "default": "",
          "description": "The authorization token (for demonstration purposes)",
          "type": "string"
        },
        "user_id": {
          "default": "",
          "description": "The user's ID extracted by the interceptor",
          "type": "string"
        },
        "user_name": {
          "default": "",
          "description": "The user's name extracted by the interceptor",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "get_personalized_greeting",
    "outputSchema": {
      "description": "Output from the personalized greeting tool.",
      "properties": {
        "greeting": {
          "description": "A personalized greeting message",
          "type": "string"
        },
        "timestamp": {
          "description": "The current time",
          "type": "string"
        },
        "user_name": {
          "description": "The user's name",
          "type": "string"
        }
      },
      "required": [
        "greeting",
        "user_name",
        "timestamp"
      ],
      "type": "object"
    }
  }
]